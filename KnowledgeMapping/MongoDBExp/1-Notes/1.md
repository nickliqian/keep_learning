```
显示所有数据库
> show dbs

显示当前使用数据库
> db

使用数据库
> use testDB

显示库中所有集合
> show collections

创建集合
> db.createCollection(name, options)


> db.raw_doc.count()
> db.raw_doc.count({"content": ""})
> db.raw_doc.find({"content": ""}).limit(1)
```
